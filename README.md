# AirlineReservationSystem
A Windows Desktop Application for managing airline flights, passengers &amp; reservations — built with C++ Win32 API and SQLite. Supports full CRUD operations with dual search methods.
